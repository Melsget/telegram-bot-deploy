services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python myfirstbot.py"
    envVars:
      - key: BOT_TOKEN
        value: